// This file is deprecated as Routing logic has moved to App.tsx
// Keeping it for reference or potential future use as a simple dashboard container
// if needed, but currently unused in the new Router architecture.

export function DashboardPage() {
    return (
        <div className="p-4">
            <h1 className="text-xl font-bold">Dashboard moved to App Router logic.</h1>
        </div>
    );
}
